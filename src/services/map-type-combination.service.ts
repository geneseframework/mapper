import { CreateOptions } from '../interfaces/create-options.interface';
import { ArrayOfPrimitiveElements, PrimitiveElement } from '../types/primitives.type';
import { Tuple } from '../types/tuple.type';

export class MapTypeCombinationService {

    static async createTypeCombination<T>(data: any, typeName: string, options: CreateOptions): Promise<any> {

    return undefined;
    }
}
