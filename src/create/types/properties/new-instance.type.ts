/**
 * Initializers which are defined by new instances
 */
// TODO : use this type to map classes with properties having initializers which are new instances
export type NewInstance = `new ${string}(${string})`;

