/**
 * Initializers which are defined by new instances
 */
// TODO : use this type to mapIfValid classes with properties having initializers which are new instances
export type NewInstance = `new ${string}(${string})`;

